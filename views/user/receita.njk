{% extends "user/layout.njk"%}

{% block head %}
 <title>Foodfy - {{item.recipe_name}}</title>
{% endblock head %}

{%block content%}
{# REFAZER ESSA PAGINA ARRUMAR O CODIGO DE HIDE #}
<div class="recipe single-page" id="{{item.id}}">
        <img src="{{item.img_url}}" alt="Imagem {{item.recipe_name}}" id="img-recipe">
        <h1 >{{item.recipe_name}}</h1>
        <h2 >{{item.author_name}}</h2>
        <div>
            <h4>Ingredientes</h4>
            <button type="button" class="hide">Fechar</button>
        </div>
        <ul class="content">
            {% for ingredient in item.ingredients %}
            <li>{{ingredient}}</li>
            {% endfor %}
        </ul>
        <div>
            <h4>Modo de Preparo</h4>
            <button type="button" class="hide">Fechar</button>
        </div>
        <ul class="content">
            {% for modo in item.steps %}
            <li>{{modo}}</li>
            {% endfor %}
        </ul>
        <div class="hide">
            <h4>Informações adicionais</h4>
            <button type="button" class="hide">Fechar</button>
        </div>
            <p class="content " >{{item.extra_information}}</p>
</div>

<script src="/scripts.js"></script>
{%endblock%}